class Card {
  constructor(cardId, matchInfo) {
    this.cardId = cardId;
    this.matchInfo = matchInfo;
    this.selected = false;
    this.matched = false;
  }
  match() {

  }
    }

    //if this instance matches -> moveToMatched()
    //this.matched = true;
  
