class Card {
  constructor(cardId, matchInfo) {
    this.cardId = cardId;
    this.matchInfo = 
    this.matched = false;
  }
  match() {
    //if this instance matches -> moveToMatched()
    //this.matched = true;
  }
}
